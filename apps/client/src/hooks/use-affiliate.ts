export const useAffiliate = () => {
  // Check if the user has an affiliate code in the URL
  // If so, store it in the local storage
  // This will allow us to use it when the user signs up
  // and attribute the sign up to the affiliate

  return {};
};
